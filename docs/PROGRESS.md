# ✅ เช็คความคืบหน้า vs TODO — พี่เงิน

> เทียบ `TODO.md` กับงานที่ทำจริง · สำหรับวางแผนนำเสนอ

---

## 🎉 ทำเสร็จแล้ว (เซสชันล่าสุด)

### ฟีเจอร์ใหม่ใหญ่ (นอก TODO — ยกระดับพี่เงินให้เหนือคู่แข่ง "ป้านวล")
- ✅ **จดเงินด้วยภาษาคน** "ก๋วยเตี๋ยว 55" / "เงินเดือน 30000" → จดให้ทันที (แม่น 100%)
- ✅ **การ์ด "จดสำเร็จ"** + ปุ่มลบ/แก้ + เลือกหมวดในการ์ด (แบบป้านวล)
- ✅ **AI ดึงข้อมูลจริงมาตอบ** (function calling) — "เดือนนี้ใช้ค่าอาหารเท่าไหร่"
- ✅ **สแกนใบเสร็จ → แยกสินค้าหลายรายการ** (7-11/ซูเปอร์)
- ✅ **ตรวจเอกสารสัญญา/ซื้อรถ** — รู้ว่าไม่ใช่ใบเสร็จ ไม่จดผิด
- ✅ **ปุ่มหยุด/ส่งซ้ำ/คัดลอก** ในแชท (ฟีลเหมือนคุยกับ Claude)
- ✅ **แนบรูปแล้วรอ** ให้พิมพ์ข้อความก่อนส่ง

### ปิดข้อใน TODO ได้
- ✅ **A2** — `parser.ts` fallback หมวด `Food` → `OtherExpense` + เขียน keyword ใหม่ครบ 32 หมวด (ค่าไฟ/ค่าเน็ต/ค่างวด/โบนัส จัดหมวดถูกแล้ว)
- ✅ **แก้บั๊กจอแดง** — id ข้อความซ้ำใน chat (duplicate key crash)
- ✅ **C1 Gamification** — ระบบแต้ม/rank + inactivity penalty (**ต้าทำ**)

### งานรวมโค้ด
- ✅ **รวมโค้ดทุกคนบน main** (โดม+ต้า+แตงกวา) ไม่ทับกัน · build ผ่าน 0 error ทั้ง backend/mobile
- ✅ แก้บั๊ก build บน main (edit_goal_screen วงเล็บหาย + prisma regenerate)

---

## ⏳ ยังต้องทำต่อ (จาก TODO)

### 🔴 A · ต้องแก้ก่อนเดโม (ส่วนใหญ่ยังค้าง — สำคัญ!)
- [ ] **ลบข้อมูลปลอมที่โผล่ตอนเดโม** — รหัส demo prefill (login), goal ปลอม 3 อัน, "Fanta Inazuma", **mock Gmail ที่หลอกว่าคุย Google**
- [ ] สูตรพยากรณ์ผิด (`app.py` หารผิดตัว → เงินเดือน 25k เป็นรายได้วันละ 25k)
- [ ] "งบรวม" นับผิด · `GET /chat` เรียงเก่าสุด · แกน Y พยากรณ์ตัดยอดติดลบ
- [ ] receiveTimeout 10s (แชท/OCR ล้มบ่อย) · pull-to-refresh พยากรณ์ว่าง · onboarding เด้งทุกครั้ง
- [ ] push แจ้งเตือนซ้ำ · subscription เตือนได้รอบเดียว

### 🔒 B · ความปลอดภัย (ก่อนขึ้นจริง) — ยังไม่เริ่ม
- [ ] OAuth ไม่เช็ค audience · JWT_SECRET default · XSS · rotate Typhoon key

### 🟡 C/D · ฟีเจอร์+เชื่อมต่อ
- [ ] forgot password (stub) · budget duration (mock) · menu settings (stub)
- [ ] **Goals ฝั่ง mobile ต่อ backend จริง** (ยังเก็บ Hive?) · FCM creds จริง

### ⚙️ E/F · DevOps + ส่งงาน
- [ ] CI · deploy staging · SQLite→Postgres · `ai/requirements.txt` ขาด deps
- [ ] **เขียน test (ยังไม่มีเลย)** · demo video · ตรวจเอกสาร SE

---

## 🎯 แนะนำก่อนนำเสนอ (เรียงความสำคัญ)

1. **ลบข้อมูลปลอม (A1)** — สำคัญสุด! กันโผล่ตอนเดโมต่อหน้ากรรมการ (รหัส demo, goal ปลอม, mock Gmail)
2. **แก้ quick wins** — receiveTimeout, สูตรพยากรณ์ (แก้ไม่กี่บรรทัด กระทบสูง)
3. **ซ้อมเดโม 3 รอบ + อัดวิดีโอสำรอง**
4. push fix build ขึ้น main ให้ทุกคน

> **สรุป:** ฟีเจอร์เด่นพร้อมโชว์แล้ว 💪 แต่ต้องเก็บกวาดข้อมูลปลอม + quick wins ก่อนขึ้นเวที · รายละเอียดเต็ม → `TODO.md`

// Basic Flutter widget test for AI Finance Coach app.
import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder smoke test — full integration tests require a running backend.
  test('placeholder test', () {
    expect(1 + 1, equals(2));
  });
}

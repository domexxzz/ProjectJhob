/**
 * เพดานจำนวนเงินที่ระบบรับได้
 *
 * ทุกฟิลด์เงินเก็บเป็น "สตางค์" ในคอลัมน์ Int ของ Postgres ซึ่งรับได้ถึง
 * 2,147,483,647 เท่านั้น ถ้าปล่อยให้ผ่าน zod ไปโดยไม่จำกัด แล้วผู้ใช้ส่ง
 * ค่ามหาศาล (เช่น 1e308) ฐานข้อมูลจะโยน error → เซิร์ฟเวอร์ตอบ 500
 * ทั้งที่จริง ๆ เป็นความผิดของข้อมูลขาเข้า (ต้องเป็น 400)
 *
 * ตั้งไว้ที่ 2,000,000,000 สตางค์ = 20 ล้านบาท ต่ำกว่าเพดาน Int เล็กน้อย
 * เผื่อที่ให้การบวกยอดรวมไม่ล้นตาม
 */
export const MAX_SATANG = 2_000_000_000;

/** 20,000,000 บาท — ไว้ใช้ในข้อความบอกผู้ใช้ */
export const MAX_BAHT = MAX_SATANG / 100;
